api.security.token.secret: ${JWT_SECRET:my-secret-key}

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/desafioUserCarDB
    username: root
    password: 123456
    diverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:ddl-auto: update
  
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  web:
    locale: pt_BR
  
